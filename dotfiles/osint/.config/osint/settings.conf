# NEXUS OSINT Configuration File

# Enable fetching of threat intelligence feeds (e.g., Tor exit nodes)
FETCH_THREAT_INTEL="true"

# Enable monitoring of local network connections
MONITOR_NETWORK="true"

# Example API Keys (leave empty or replace with real ones if needed)
# SHODAN_API_KEY=""
# VIRUSTOTAL_API_KEY=""
